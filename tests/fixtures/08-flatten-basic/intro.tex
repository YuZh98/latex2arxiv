\section{Introduction}
This intro section is referenced from the main file and inlined into the
flattened output. The same content survives a normal (non-flatten) run as
a separate fragment file in the zip.
