\documentclass[../main]{subfiles}

\begin{document}

\section{Chapter One}
This subfile lives in a subdirectory; its subfiles-class preamble points
back at the project root via the relative path. Under flatten only the
body between begin-document and end-document is inlined into the merged
main file; the preamble and wrapper are dropped.

\end{document}
